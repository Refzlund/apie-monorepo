import { endpoint } from '$'

export const GET = endpoint({}, pipe => pipe(1))
export const PUT = endpoint({}, pipe => pipe(1))
export const PATCH = endpoint({}, pipe => pipe(1))
export const DELETE = endpoint({}, pipe => pipe(1))